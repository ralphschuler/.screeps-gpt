# Skip optional dependencies to avoid native module build issues
# The ws library's native performance modules (bufferutil, utf-8-validate)
# are not required for functionality and can fail on systems with Python 3
optional=false
