{
  "name": "screeps-gpt",
  "version": "0.5.0",
  "description": "Autonomously evolving Screeps AI with automated development and deployment workflows.",
  "license": "MIT",
  "author": "OpenAI Automations",
  "type": "module",
  "packageManager": "bun@1.3.1",
  "engines": {
    "node": ">=18.0.0 <=22.x",
    "bun": ">=1.0.0"
  },
  "scripts": {
    "build": "tsx scripts/build.ts",
    "build:docs-site": "tsx scripts/build-docs-site.ts",
    "build:watch": "tsx scripts/build.ts --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier .",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "bun run test:unit",
    "test:actions": "tsx scripts/test-actions.ts",
    "test:unit": "vitest run --dir tests/unit",
    "test:e2e": "vitest run --dir tests/e2e",
    "test:mockup": "vitest run --dir tests/mockup",
    "test:regression": "vitest run --dir tests/regression",
    "test:coverage": "vitest run --coverage",
    "deploy": "tsx scripts/deploy.ts",
    "version:bump": "tsx scripts/bump-version.ts",
    "version:bump-semantic": "tsx scripts/bump-version-semantic.ts",
    "versions:update": "tsx scripts/update-version-index.ts",
    "analyze:system": "tsx scripts/evaluate-system.ts",
    "prepare": "husky",
    "docker:build": "docker compose build",
    "docker:test": "docker compose run --rm test",
    "docker:test:unit": "docker compose run --rm test npm run test:unit",
    "docker:test:e2e": "docker compose run --rm test npm run test:e2e",
    "docker:test:mockup": "docker compose run --rm mockup",
    "docker:test:regression": "docker compose run --rm test npm run test:regression",
    "docker:test:coverage": "docker compose run --rm test npm run test:coverage",
    "docker:build:ai": "docker compose run --rm build",
    "docker:lint": "docker compose run --rm lint",
    "docker:format": "docker compose run --rm format",
    "docker:dev": "docker compose up dev",
    "docker:shell": "docker compose run --rm test bash"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "screeps-api": "^1.16.1",
    "semver": "^7.7.3",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@types/node-fetch": "^2.6.13",
    "@types/screeps": "^3.3.8",
    "@types/semver": "^7.5.8",
    "@typescript-eslint/eslint-plugin": "^8.46.2",
    "@typescript-eslint/parser": "^8.46.2",
    "@vitest/coverage-v8": "^4.0.1",
    "bun-plugin-screeps": "^1.0.0",
    "esbuild": "^0.25.11",
    "eslint": "^9.38.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.29.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "marked": "^16.4.1",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6",
    "typescript": "^5.4.3",
    "vitest": "^4.0.1"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{js,mjs,cjs,json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
