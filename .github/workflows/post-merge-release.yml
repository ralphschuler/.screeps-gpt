---
name: Post Merge Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Skip if commit is a release (prevents recursive execution)
    if: >-
      !contains(github.event.head_commit.message, 'chore(release):')
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Install dependencies
        run: bun install --no-optional

      - name: Lint with fixes
        run: bun run lint:fix

      - name: Format with write
        run: bun run format:write

      - name: Bump version (semantic)
        id: bump
        shell: bash
        run: |
          # Use semantic versioning based on conventional commits
          VERSION=$(bun run version:bump-semantic 2>&1 | \
            grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | tail -n 1)
          BUMP_TYPE=$(bun run version:bump-semantic 2>&1 | \
            grep 'Bump type:' | awk '{print $3}' || echo "patch")
          if [ -z "$VERSION" ]; then
            echo "Version bump did not return a version"; exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "bump_type=$BUMP_TYPE" >> "$GITHUB_OUTPUT"

      - name: Release changelog
        shell: bash
        run: |
          VERSION="${{ steps.bump.outputs.version }}"
          DATE=$(date +%Y-%m-%d)
          npm run version:release "$VERSION" "$DATE"

      - name: Refresh version index
        run: bun run versions:update

      # Commit version bump and changelog to a unique branch instead of main
      - name: Commit version bump to branch
        id: commit
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: >-
            chore(release): bump version to ${{
            steps.bump.outputs.version }} [skip ci]
          branch: auto/release-${{ github.run_id }}
          create_branch: true
          commit_user_name: github-actions[bot]
          commit_user_email: >-
            41898282+github-actions[bot]@users.noreply.github.com
          skip_fetch: false
          push_options: '--force'

      # Create Pull Request for release artifacts
      - name: Create Pull Request for release artifacts
        id: create_pr
        if: steps.commit.outputs.changes_detected == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: auto/release-${{ github.run_id }}
          base: main
          title: >-
            chore(release): bump version to ${{
            steps.bump.outputs.version }} (run ${{ github.run_id }})
          body: |
            ## Automated Release PR

            This PR contains automated release artifacts generated by the
            Post Merge Release workflow.

            **Version:** ${{ steps.bump.outputs.version }}
            **Bump type:** ${{ steps.bump.outputs.bump_type }}
            **Workflow run:** ${{ github.run_id }}
            **Reference commit:** ${{ github.sha }}

            ### Changes included:
            - Version bump to ${{ steps.bump.outputs.version }}
            - Updated CHANGELOG.md
            - Updated version index
            - Lint and format fixes

            ### Next steps:
            1. Review the changes in this PR
            2. Merge this PR to main
            3. The deploy workflow will automatically trigger on merge
          labels: |
            automated
            type/chore
            state/pending

      # Create and push tag (only after PR is created)
      # Note: This will create the tag on the PR branch, which will be
      # available after merge
      - name: Create and push tag
        if: >-
          steps.bump.outputs.version != ''
          && steps.commit.outputs.commit_hash != ''
          && steps.create_pr.outputs.pull-request-number != ''
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.COPILOT_TOKEN }}
        run: |
          VERSION="${{ steps.bump.outputs.version }}"
          TAG="v${VERSION}"
          SHA="${{ steps.commit.outputs.commit_hash }}"

          # Check if tag already exists
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists"
            exit 0
          fi

          # Create the tag (push will happen after PR merge)
          git tag "$TAG" "$SHA"
          git push origin "$TAG" || {
            # Tag might have been created by another workflow run
            echo "Tag $TAG could not be pushed (may exist remotely)"
            exit 0
          }

      # Create GitHub Release using native API
      - name: Create GitHub Release
        if: >-
          steps.bump.outputs.version != ''
          && steps.commit.outputs.commit_hash != ''
          && steps.create_pr.outputs.pull-request-number != ''
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.COPILOT_TOKEN }}
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const version = '${{ steps.bump.outputs.version }}';
            const tag = `v${version}`;
            const sha = '${{ steps.commit.outputs.commit_hash }}';
            const bumpType = '${{ steps.bump.outputs.bump_type }}';

            // Generate release notes using GitHub's API
            let releaseNotes = '';
            try {
              const { data } = await github.rest.repos.generateReleaseNotes({
                owner,
                repo,
                tag_name: tag,
                target_commitish: sha
              });
              releaseNotes = data.body || '';
            } catch (e) {
              core.warning(`Could not generate release notes: ${e.message}`);
              releaseNotes = `Automated release ${tag}`;
            }

            // Check if release already exists
            try {
              const { data: existingRelease } = await
                github.rest.repos.getReleaseByTag({
                  owner,
                  repo,
                  tag
                });
              core.info(`Release ${tag} already exists: ${
                existingRelease.html_url}`);
              return;
            } catch (err) {
              if (err.status !== 404) throw err;
              // Release doesn't exist, create it
            }

            // Create the release
            const { data: release } = await
              github.rest.repos.createRelease({
                owner,
                repo,
                tag_name: tag,
                target_commitish: sha,
                name: `Release ${tag}`,
                body: `## ${tag}\n\nVersion bump type: **${
                  bumpType}**\n\n${releaseNotes}`,
                draft: false,
                prerelease: false
              });

            core.info(`Created release: ${release.html_url}`);
